% Shared: embed third-party layout scans (reference-*.jpeg/png). Not j.adezhao.jpg.
% \input{} in preamble after geometry; keep \usepackage{hyperref} after this file.
\usepackage{graphicx}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf,justification=raggedright,singlelinecheck=false}

\newcommand{\JadeLayoutRefDisclaimerMini}{%
  \noindent\begin{minipage}{0.96\linewidth}%
  \small\textit{\textbf{Layout reference only.} Third-party document scan for visual structure. Visible names and body text in the image are not Jade Zhao. All typeset \LaTeX{} content before this page is Jade Zhao.}%
  \end{minipage}\par\vspace{0.75em}%
}

% One figure only (use after a shared appendix note, e.g. in jade-zhao-profile-full.tex).
\newcommand{\JadeLayoutRefScanOnly}[2]{%
  \begin{figure}[p]%
    \centering%
    \includegraphics[width=\linewidth]{#1}%
    \caption{#2}%
  \end{figure}%
}

% New page + disclaimer + figure (standalone documents).
\newcommand{\JadeLayoutRefFigure}[2]{%
  \clearpage
  \JadeLayoutRefDisclaimerMini
  \JadeLayoutRefScanOnly{#1}{#2}%
}
